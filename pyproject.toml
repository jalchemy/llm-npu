[project]
name = "app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "modelscope>=1.36.1",
    "questionary>=2.1.1",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
